lic-open(1) -- Opens the source directory for a gem in your lic
=====================================================================

## SYNOPSIS

`lic open` [GEM]

## DESCRIPTION

Opens the source directory of the provided GEM in your editor.

For this to work the `EDITOR` or `LIC_EDITOR` environment variable has to
be set.

Example:

    lic open 'rack'

Will open the source directory for the 'rack' gem in your lic.
